<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:mvc="http://www.springframework.org/schema/mvc"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="
http://www.springframework.org/schema/beans
http://www.springframework.org/schema/beans/spring-beans-4.1.xsd
http://www.springframework.org/schema/context
http://www.springframework.org/schema/context/spring-context-4.1.xsd
http://www.springframework.org/schema/mvc
http://www.springframework.org/schema/mvc/spring-mvc-4.1.xsd">

	<!--启动基于注解的配置 -->
	<mvc:annotation-driven />

	<!-- 扫描所有ctrl目录下面的控制器controller -->
	<context:component-scan base-package="com"></context:component-scan>

	<bean id="propertyConfigurer"
		class="org.springframework.beans.factory.config.PropertyPlaceholderConfigurer">
		<property name="locations">
			<list>
				<value>classpath:*.properties</value>
			</list>
		</property>
	</bean>

	<bean id="dataSource"
		class="org.apache.commons.dbcp2.BasicDataSource"
		destroy-method="close">
		<property name="driverClassName"
			value="${spring.datasource.driver-class-name}" />
		<property name="url" value="${spring.datasource.url}" />
		<property name="username"
			value="${spring.datasource.username}" />
		<property name="password"
			value="${spring.datasource.password}" />
		<property name="initialSize" value="2" />
		<property name="maxTotal" value="5" />
		<property name="maxIdle" value="1" />
		<property name="maxWaitMillis" value="-1" />
	</bean>
	<bean id="jdbcTemplate"
		class="org.springframework.jdbc.core.JdbcTemplate">
		<property name="dataSource" ref="dataSource"></property>
	</bean>
	
	<bean id="connManager" class="com.http.WxPoolingHttpClientConnectionManager" destroy-method="close">
		<property name="maxTotal" value="${http.pool.maxTotal}"></property>
		<property name="defaultMaxPerRoute" value="${http.pool.defaultMaxPerRoute}"></property>
	</bean>
	
	<bean id="requestConfigBuilder" class="org.apache.http.client.config.RequestConfig" factory-method="custom">
		<!-- 从连接池中获取到连接的最长时间 -->
		<property name="connectionRequestTimeout" value="${http.request.connectionRequestTimeout}"/>
		<!-- 创建连接的最长时间 -->
		<property name="connectTimeout" value="${http.request.connectTimeout}"/>
		<!-- 数据传输的最长时间 -->
		<property name="socketTimeout" value="${http.request.socketTimeout}"/>
		<!-- 提交请求前测试连接是否可用 -->
		<property name="staleConnectionCheckEnabled" value="${http.request.staleConnectionCheckEnabled}"/>
	</bean>
	
	<bean id="requestConfig" factory-bean="requestConfigBuilder" factory-method="build"></bean>
	
	<bean id="httpClientBuilder" class="org.apache.http.impl.client.HttpClientBuilder" factory-method="create">
		<property name="connectionManager" ref="connManager"></property>
		<property name="defaultRequestConfig" ref="requestConfig"></property>
	</bean>
	<bean id="closeableHttpClient" factory-bean="httpClientBuilder" factory-method="build"></bean>
	<!-- 
	<bean class="com.http.ThreadService" destroy-method="close">
		<property name="connManager" ref="connManager"></property>
	</bean>
	 -->
	 
	 <bean class="com.execpt.WxHandlerExceptionResolver"></bean>
</beans>